<p id="notice"><%= notice %></p>

<h1>Contatos de <%= @client.full_name %></h1>


<div class="float-right">
  <%= link_to 'Novo Contato', new_client_contact_path,  class: "btn btn-primary active" %>
</div>


<table class="table">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Email</th>
      <th>Telefone</th>
      <th scope="col" colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contacts.each do |contact| %>
      <tr>
        <td><%= contact.full_name %></td>
        <td><%= contact.email %></td>
        <td><%= contact.phone %></td>
        <td><%= link_to 'Editar', edit_client_contact_path(contact.client, contact) , class: "btn btn-primary btn-sm active"  %></td>
        <td><%= link_to 'Remover', client_contact_path(contact.client, contact), method: :delete, data: { confirm: 'Você tem certeza?' }, class: "btn btn-danger btn-sm active" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Voltar', clients_path, class: "btn btn-secondary btn-sm active"  %></td> 
<br>

